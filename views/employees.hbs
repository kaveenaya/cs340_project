<!-- index.hbs -->

{{!-- Create a table --}}
<table id="employees-table">

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr data-value={{this.employeeID}}>
            <td>{{this.employeeID}}</td>
            <td>{{this.name}}</td>
             <td><button onclick="deleteEmployee({{this.employeeID}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
<script src="./js/delete_employees.js"></script>

<!-- /views/index.hbs -->
<h2>Adding Data using an HTML form</h2>
<p>To add a new employee, please enter their information below nad click `Submit`!</p>
<form id="add-employee-form" method="POST" action="/add-employee-form">
    <label for="input-id">ID: </label>
    <input type="number" name="input-id" id="input-id">

    <label for="input-name">Name: </label>
    <input type="text" name="input-name" id="input-name">

    <input type="submit">
</form>


<h2>Updating Employee Form</h2>
<p>Select the employeeID from the dropdown below and update its information:</p>
<fieldset>
    <form id="update-employee-form">

        <label for="update-employee-id">Select the employee ID: </label>
        <select name="update-employee-id" id="update-employee-id">
            {{#each employeeIDS}}
            <option value="{{this.employeeID}}">{{this.employeeID}}</option>
            {{/each}}
        </select>


        <label for="update-employee-name"> employee Name: </label>
        <input type="text" name="update-employee-name" id="update-employee-name">
    

        <input type="submit" onclick="return confirm('You are about to update the employee. Are you Sure?')"/>

    </form>
</fieldset>
<script src="./js/update_employee.js"></script>
